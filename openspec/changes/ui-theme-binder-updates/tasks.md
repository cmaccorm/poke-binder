## 1. Theme Configuration

- [x] 1.1 Update `src/app/globals.css` with dark charcoal colors and adjust red variable if needed for glowing text.

## 2. Header Updates

- [x] 2.1 Update `src/components/Header.tsx` to make the "poké-binder" text red with a glowing text-shadow.

## 3. Shelf UI Refinements

- [x] 3.1 Update `src/components/Shelf.tsx` to remove the "My Binders" heading and associated red icon.
- [x] 3.2 Adjust the layout of the "+ New Binder" button in `src/components/Shelf.tsx` to ensure it still looks positioned correctly without the heading.

## 4. Binder Card Enhancements

- [x] 4.1 Update `src/components/BinderCard.tsx` to add a vertical dark gradient overlay simulating a curved book spine on the left.
- [x] 4.2 Update `src/components/BinderCard.tsx` to add a thick white/off-white right border to simulate pages.
- [x] 4.3 Update `src/components/BinderCard.tsx` to enhance the overall drop shadow for a more pronounced 3D effect.
